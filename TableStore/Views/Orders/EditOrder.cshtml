@*@{
    ViewData["Title"] = "Создание / Обновление заказа";
}
@model Order

<div class="p-5 text-center bg-light">
    <h2 class="mb-3">Создание / Обновление заказа</h2>
</div>

<form asp-action="AddOrUpdateOrder" method="post">
    <div class="form-group">
        <label asp-for="CustomerName">Имя клиента</label>
        <input asp-for="CustomerName" class="form-control" />
    </div>
    <div class="form-group">
        <label asp-for="Address">Адрес</label>
        <input asp-for="Address" class="form-control" />
    </div>
    <div class="form-group">
        <label asp-for="State">Страна</label>
        <input asp-for="State" class="form-control" />
    </div>
    <div class="form-group">
        <label asp-for="ZipCode">ZipCode</label>
        <input asp-for="ZipCode" class="form-control" />
    </div>
    <div class="form-check">
        <label class="form-check-label">
            <input type="checkbox" asp-for="IsShipped" class="form-check-input" />
            Отправлен
        </label>
    </div>

    <h6 class="mt-1 р-2 bg-primary text-white text-center">
        Заказанные товары
    </h6>
    <div class="container-fluid">
        <div class="row">
            <div class="col font-weight-bold">Товар</div>
            <div class="col font-weight-bold">Цена</div>
        </div>
        @{
            int counter = 0;
        }
        @foreach (Position position in ViewBag.Positions)
        {
            <input type="hidden" name="positions[@counter].Id" value="@position.Id" />
            <input type="hidden" name="positions[@counter].TableId" value="@position.TableId" />
            <input type="hidden" name="positions[@counter].OrderId" value="@position.OrderId" />
            <div class="row mt-1">
                <div class="col">@position.Table.Model</div>
                <div class="col">@position.SellingPrice</div>
                <div class="col">
                    <input type="number" name="positions[@counter].Count" value="@position.Count" />
                </div>
            </div>
            counter++;
        }
    </div>
    <div class="form-group text-center">
        <button class="btn btn-primary" type="submit">Сохранить</button>
        <a asp-action="Index" class="btn btn-secondary">Отмена</a>
    </div>
</form>*@